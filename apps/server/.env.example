# Path to the bun:sqlite database file.
# "../../local.db" resolves to the repo root from both apps/server and packages/db.
DATABASE_URL=../../local.db

# Allowed origin for the web dashboard.
CORS_ORIGIN=http://localhost:5173

NODE_ENV=development

# Python ML inference service.
ML_URL=http://localhost:8000
